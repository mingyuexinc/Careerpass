# 验证计划

- 验证 JWT 配置、最小密码输入约束与 scrypt 哈希。
- 验证注册、重复注册、登录、`/auth/me` 的响应和错误语义。
- 使用真实 PostgreSQL 验证 Alembic 升级、原子创建和身份解析。
- Redis 认证限流已移出认证模块，不规划相关 `429` 或 `503` 验证。
- 验证简单非空密码可通过，空密码和超过 128 字符密码被拒绝；确认密码不出现在模型表示或接口响应中。
- 发布前复验迁移可重复升级至 `head`，且敏感值不出现在日志和响应中。
