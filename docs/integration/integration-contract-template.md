# Integration Contract：<场景名称>

> 本文档定义前端与后端共同遵守的跨端联调契约。
>
> 业务语义引用业务基线和 Slice 规格；本文件只记录跨端调用、响应、状态和错误。契约未锁定前，前端 Mock 和后端 Implement 不得以各自假设继续扩大范围。

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| Contract ID | `<例如 IC-S02-JD-UPLOAD>` |
| 版本 | `<major.minor>` |
| 关联 Slice | `<Slice ID>` |
| 关联 Integration Scenario | `<IS ID>` |
| Producer | `<后端 Slice / 服务>` |
| Consumer | `<正式前端页面或下游服务>` |
| 状态 | `draft` / `locked` / `superseded` |

## 2. 用户场景与边界

- 角色和前置条件：`<内容>`
- 用户触发：`<内容>`
- 本契约产生的用户可观察结果：`<内容>`
- 不在本契约中的结果：`<内容>`
- 业务事实和 Slice 规格：`<引用>`

## 3. 请求契约

| 项目 | 约定 |
| --- | --- |
| 方法与路径 | `<内容>` |
| 身份与授权 | `<服务端校验和资源归属>` |
| 请求格式 | `<JSON / multipart / query>` |
| 字段和约束 | `<字段、类型、必填、限制>` |
| 幂等键 | `<无 / 规则>` |

## 4. 响应契约

所有 API 响应遵循 `{code, msg, data}`。

| 场景 | HTTP / code | `data` 结构 | 前端可观察结果 |
| --- | --- | --- | --- |
| 成功 | `<内容>` | `<内容>` | `<内容>` |
| 重复/幂等成功 | `<内容>` | `<内容>` | `<内容>` |
| 业务失败 | `<内容>` | `<内容>` | `<内容>` |
| 未授权/无权 | `<内容>` | `<内容>` | `<内容>` |

字段必须注明来源和状态拥有者。异步结果不得伪装成同步完成结果。

## 5. 状态与异步行为

```text
<状态变化或时序>
```

| 状态 | 产生者 | 消费者 | 页面表现 | 可执行操作 |
| --- | --- | --- | --- | --- |
| `<状态>` | `<来源>` | `<来源>` | `<表现>` | `<操作>` |

## 6. 错误、重试与安全边界

- 错误分类和用户文案：`<内容>`
- 重试和重复提交：`<内容>`
- 不得返回、记录或追踪的内容：`<内容>`
- 跨端日志和诊断字段：`<关联 ID、阶段、状态等>`

## 7. 示例与兼容性

### 7.1 Contract 示例

```json
{
  "code": 200,
  "msg": "<message>",
  "data": {}
}
```

### 7.2 兼容规则

- 兼容字段只能新增为可选字段；
- 删除、改名、改变含义或改变状态语义必须升 Major 版本；
- Contract 变化必须回退到 Slice Design，并同步更新前端 Mock、后端 Schema 和 Integration Scenario。

## 8. 锁定记录

| 日期 | 变化 | 影响 | 回退 Gate | 结论 |
| --- | --- | --- | --- | --- |
| `<日期>` | `<变化>` | `<前端/后端/场景>` | `<Gate>` | `<内容>` |
