# 任务拆分（阶段 4 增量重划；阶段 4 已通过）

> 本文件保留历史任务和当前新边界的阶段 4 拆分。下方“本次增量任务”是阶段 4 通过后的唯一执行计划；阶段 4 已完成任务拆分门禁，但不代表阶段 5 编码已启动或模块已完成。`ResumeParseRequestV1@v1` 已锁定，CHG-020、CHG-021 阶段 3 均已通过。

## 1. 阶段 4 前置门禁

| 门禁项 | 要求 | 当前状态 |
| --- | --- | --- |
| CHG-020 阶段 3 | 本包方案设计和变更记录经开发者收口并标记 `passed` | 已满足 |
| CHG-021 阶段 3 | 消费者方案设计经开发者收口并标记 `passed` | 已满足 |
| 跨包契约 | `ResumeParseRequestV1@v1` 为 `locked`，哈希一致，双方批准已登记 | 已满足 |
| 任务内容 | 不重新裁决字段、生产者、消费者、事务或触发语义 | 已满足 |
| 阶段 4 台账 | 两包依赖门禁满足后，启动本包阶段 4 并将任务状态确认可执行 | 已通过 |

## 2. 当前模块边界、能力地图与依赖图

### 2.1 模块边界

| 组件 | CHG-020 本次责任 | 明确不负责 |
| --- | --- | --- |
| G2 API/Service | 认证身份、上传输入校验、对象临时写入、外层事务编排、统一响应 | 不调用 G3 Service、Dispatcher、Worker、MinerU 或 Qwen |
| `CandidatePreparationRepository` | 在调用方提供的同一 Session/事务中创建或复用 `StoredFileObject`、`Resume` 和上传幂等关系 | 不独立提交事务；不访问 G3 Repository |
| `AsyncTaskRepository` | 在同一事务中创建或复用 `resume_parse` 的 queued `AsyncTaskRun` | 不投递 Redis/Celery；不执行解析 |
| 本地对象存储 | 受控临时写入、`ready` 状态确认和失败清理 | 不提供下载、URL、路径或对象键 |
| CHG-021 G3 | 消费 G2 已提交的 queued 任务并完成解析链路 | 不属于本次 G2 任务实现范围 |

### 2.2 技术能力地图

```text
CurrentIdentity
  -> G2 API/Service
  -> 本地对象存储临时写入与 ready 校验
  -> 外层 PostgreSQL 事务
       -> CandidatePreparationRepository
       -> AsyncTaskRepository.create_or_get_queued_resume_task
       -> StoredFileObject + Resume + upload idempotency + queued AsyncTaskRun
  -> 201 / UPLOAD_ACCEPTED / parse_status=processing
  -> CHG-021 后续消费（本次不实现）
```

本次只复用阶段 2 已验证的认证归属链、PostgreSQL/Repository 和本地对象存储，不新增 Redis、Celery、Dispatcher、MinerU 或 Qwen 任务。

### 2.3 依赖图

```text
CHG-020 阶段 3 passed
  + CHG-021 阶段 3 passed
  + ResumeParseRequestV1@v1 locked + hash 一致
      -> T4-G2-01 外层事务协调
          -> T4-G2-02 queued 任务 Repository
          -> T4-G2-03 归属/ready/资源建档
          -> T4-G2-04 上传幂等与任务幂等
          -> T4-G2-05 API 受理响应
          -> T4-G2-06 失败清理与边界检查
          -> T4-G2-07 阶段 5/6 证据映射
```

## 3. 本次增量任务清单

以下任务只覆盖新边界导致的差异，不重复实现历史上传、对象存储、解析 Worker 或画像链路。每项预估不超过 4 小时；阶段 4 通过后统一为“待执行”，实际编码仍须遵守阶段 5 门禁。

| 编号 | 任务 | 类型 | 依赖 | 预估 | 状态 | 验收条件 |
| --- | --- | --- | --- | --- | --- | --- |
| T4-G2-01 | 重构 G2 外层事务协调：让 Service/Repository 在同一 PostgreSQL 事务中提交 `StoredFileObject`、`Resume`、上传幂等关系和 queued 任务 | 事务/架构 | 阶段 3 双方通过 | ≤4h | 待执行 | 不再由 `create_resume()` 内部独立提交；事务提交前不返回 201；任一写入失败整体回滚 |
| T4-G2-02 | 增加 `AsyncTaskRepository.create_or_get_queued_resume_task` 设计任务：固定 `task_type=resume_parse`、`resource_type=resume`、`resource_id=resume_id`、`task_version=v1`、`status=queued`、`celery_task_id=NULL` 和任务幂等键 | Repository | T4-G2-01 | ≤4h | 待执行 | 同一 `resume_id + v1` 只保留一个有效任务；唯一约束冲突可安全复用；不调用 Dispatcher/Worker |
| T4-G2-03 | 收敛对象状态与候选人归属校验：确认简历归属当前 `candidate_id`、对象存在且为 `ready` 后才允许建档和任务占位 | 权限/资源 | T4-G2-01 | ≤4h | 待执行 | 越权、对象缺失或非 `ready` 时不创建有效任务；不泄露路径、对象键或正文 |
| T4-G2-04 | 统一上传幂等与任务幂等：处理相同请求重放、同键不同内容冲突及并发任务创建 | 幂等/并发 | T4-G2-02、T4-G2-03 | ≤4h | 待执行 | 相同幂等请求复用 `resume_id` 和 queued 任务；冲突返回 409；不创建第二份任务或覆盖既有资源 |
| T4-G2-05 | 固化正式简历 API 受理响应和状态映射 | API/边界 | T4-G2-01、T4-G2-04 | ≤4h | 待执行 | 返回 `201 / UPLOAD_ACCEPTED`、`resume_id`、`parse_status=processing`；不把解析成功、失败码或画像结果提前返回 |
| T4-G2-06 | 完成 G2 事务失败、对象清理和静态边界检查任务 | 可靠性/治理 | T4-G2-01~T4-G2-05 | ≤4h | 待执行 | 回滚后无悬挂简历、任务或可读无引用对象；G2 无 G3 Service/Dispatcher/Worker/MinerU/Qwen 依赖 |
| T4-G2-07 | 将新边界映射到阶段 5/6 验收证据并更新变更材料 | 验证准备 | T4-G2-01~T4-G2-06 | ≤4h | 待执行 | 形成合法上传、幂等、越权、非 ready、事务失败、未知字段和无 G3 调用的验证清单；不伪记测试通过 |

## 4. 本次明确不拆分的历史任务

- 不重新拆分或实现 CHG-021 的 Dispatcher、Redis、Celery Worker、租约、MinerU、Qwen、画像写入和解析终态任务。
- 不新增 handoff 表、任务 API、路径/URL/对象键字段、受控读取授权字段或契约幂等字段。
- 不重复实现已经有证据且未受新边界影响的基础上传、对象摘要去重、资料列表和认证归属能力；它们只在阶段 6 做必要回归。
- 不把历史子任务 8–11、R1–R3 的“已完成”状态迁移为当前阶段 4/5/6 通过状态。

## 5. 阶段 4 关闭条件

- CHG-020、CHG-021 阶段 3 均为 `passed`，契约版本和哈希与注册表一致。
- 上述 T4-G2-01 至 T4-G2-07 的粒度、依赖和验收条件经开发者确认。
- 任务表不包含契约裁决、生产者/消费者重新决定或未验证外部技术选型。
- 阶段 4 通过后，才允许进入阶段 5；阶段 4 本身不代表代码实现或模块完成。

## 历史任务清单（仅作证据索引）

以下“已完成”子任务、旧的 8–11 和 R1–R3 记录形成于治理迁移前。它们不覆盖当前 G2 同事务创建/复用 queued 任务的阶段 4 拆分，也不能替代联合契约锁定、阶段 3 或阶段 4 门禁。

## 已完成

| 子任务 | 交付 | 状态 |
| --- | --- | --- |
| 0-3 | 资料上传、对象存储、迁移、受控 API 与验证 | 已完成 |
| 4 | 可靠入队、Dispatcher 与执行租约 | 已完成 |
| 5 | MinerU MCP 适配器 | 已完成 |
| 6 | Qwen 严格结构化画像适配器 | 已完成 |
| 7 | 租约保护的解析终态原子写入 | 已完成，子任务 8 中迁移其模块归属 |

## 子任务 8：模块边界整改与版本化契约（历史记录）

| 顺序 | 任务 | 类型 | 本次方式 | 依赖 | 预估 | 验收条件 |
| --- | --- | --- | --- | --- | --- | --- |
| 8.1 | 定义 `ResumeParseRequestV1` 与文档解析画像契约 | 契约 | 本期必须 | 已有 v1 数据模型 | <=4h | 契约禁止额外字段，资料模块只提交请求 |
| 8.2 | 将解析任务提交、画像查询、终态持久化迁移至文档解析边界 | 业务边界 | 本期必须 | 8.1 | <=4h | 候选人资料准备不读取/写入 `candidate_profiles` 或解析终态 |
| 8.3 | 迁移画像 API 处理器与依赖装配，保持现有 URL | API | 本期必须 | 8.2 | <=4h | 画像查询只经文档解析 Service/Repository，并保留候选人归属校验 |
| 8.4 | 边界单元测试、真实数据库回归与变更证据更新 | 验证 | 本期必须 | 8.1-8.3 | <=4h | Ruff、完整单元测试、隔离集成测试和边界检索均通过 |

## 子任务 9：简历解析 Worker 编排（历史记录，归属 CHG-021）

| 顺序 | 任务 | 类型 | 本次方式 | 依赖 | 预估 | 验收条件 |
| --- | --- | --- | --- | --- | --- | --- |
| 9.1 | 实现租约驱动的简历解析编排 Service | 业务编排 | 本期必须 | 子任务 4-8 | <=4h | 每次执行先领取租约；无租约的重复消息无副作用 |
| 9.2 | 注册固定 `careerpass.resume_parse` Celery Worker 任务 | 异步运行时 | 本期必须 | 9.1、已验证 MinerU/Qwen 适配器 | <=4h | 仅接收 `task_run_id`；从受控 Repository 读取简历，禁止客户端路径/URL |
| 9.3 | 实现受控失败分类、有限重试和终态调用 | 可靠性 | 本期必须 | 9.1-9.2 | <=4h | 可重试故障先释放匹配租约再退避重投；确定性失败或重试耗尽仅写安全失败码 |
| 9.4 | Worker 编排单元回归与变更证据更新 | 验证 | 本期必须 | 9.1-9.3 | <=4h | 覆盖成功、重复消息、可重试、确定性失败、重试耗尽和任务注册 |

## 后续，不属于子任务 9

## 子任务 10：候选人资料准备模块验收与收口（历史记录）

| 顺序 | 任务 | 类型 | 模块归属 | 依赖 | 预估 | 验收条件 |
| --- | --- | --- | --- | --- | --- | --- |
| 10.1 | 验证上传、幂等、受控对象存储和本人资源查询 | 模块集成 | 候选人资料准备 | 子任务 0-3、8 | <=4h | 简历与附加资料不泄露原文/路径；其他候选人无法读取；解析状态仅为受控资源状态 |
| 10.2 | 验证 `ResumeParseRequestV1` 的原子提交与 Dispatcher 可靠投递 | 模块集成 | 候选人资料准备 + 异步技术使能 | 子任务 4、8 | <=4h | 每份正式简历在同一事务中拥有固定 v1 请求/任务；Dispatcher 可发布固定任务名；重复上传或发布中断不产生重复业务资源 |
| 10.3 | 候选人资料准备模块文档与验收记录收口 | 模块验收 | 候选人资料准备 | 10.1-10.2 | <=4h | 模块不拥有画像、文档读取、MinerU/Qwen、Worker 执行或下游准入；真实依赖测试结果如实记录 |

> 子任务 10 的完成条件止于“简历解析请求已经可靠交给文档解析模块”。它不等待、读取或断言 MinerU/Qwen 的执行结果，也不以画像成功作为资料准备模块的验收前置。

## 原任务 11：已迁移至 CHG-2026-021（历史索引）

> 自 2026-07-28 起，下列任务的计划、实现和验收责任均由 `CHG-2026-021-document-parsing-mvp` 承担。本 CHG-020 在子任务 10 收口后不再承载文档解析模块的后续开发。

| 顺序 | 任务 | 类型 | 模块归属 | 依赖 | 预估 | 验收条件 |
| --- | --- | --- | --- | --- | --- |
| 11.1 | 隔离运行时验证 Dispatcher → Celery Worker → 租约领取 | 模块集成 | 文档解析 + 异步技术使能 | 子任务 4、7-9 | <=4h | 真实 PostgreSQL、Redis、Dispatcher 与 Worker 可连通；重复/迟到消息不产生重复画像或终态写入 |
| 11.2 | 真实 MinerU → Qwen → 原子画像/终态全链路验证 | 外部集成 | 文档解析 | 11.1、子任务 5-6 | <=4h | 受控脱敏 PDF 从 `processing` 到 `succeeded`；仅写入已校验画像；不记录原文、路径、凭证或原始响应 |
| 11.3 | 故障链路验证 | 外部/模块集成 | 文档解析 | 11.1-11.2 | <=4h | 存储不可用、超时、429/5xx、不可读 PDF、Schema 失败、重试耗尽和旧租约写入均符合失败分类、有限重试和原子性约束 |
| 11.4 | 文档解析模块及 CHG-020 最终收口 | 模块验收 | 文档解析 | 11.1-11.3 | <=4h | 更新外部预验证、设计、测试报告和发布证据；仅在真实依赖验收通过后标记对应门禁通过 |

> 子任务 11 的业务闭环为：`ResumeParseRequestV1` → Dispatcher → Worker → MinerU → Qwen → Pydantic/业务校验 → `candidate_profiles` 与简历/任务原子终态。该闭环属于文档解析模块，不属于候选人资料准备模块。

## 边界修订任务：候选人资料准备实现纠偏（历史记录）

| 顺序 | 任务 | 类型 | 依赖 | 预估 | 状态 | 验收条件 |
| --- | --- | --- | --- | --- | --- | --- |
| R1 | 移除候选人资料 Repository 的解析请求提交和解析依赖装配 | 编码 | 阶段 3 方案设计 | <=4h | 已完成 | 无 `ResumeParseRequestV1` 提交回调；不注入 `DocumentParsingRepository` |
| R2 | 收敛简历 Schema、Service、API 响应为上传元数据 | 编码 | R1 | <=4h | 已完成 | 无 `parse_status`/`failure_code`；上传消息不含解析语义 |
| R3 | 更新候选人资料边界测试并执行全量回归 | 验证 | R1-R2 | <=4h | 已完成 | 全量测试通过，覆盖率达到 80%，解析模块测试保持独立 |
