# 验证计划

- 验证 JWT 配置、密码复杂度与 scrypt 哈希。
- 验证注册、重复注册、登录、`/auth/me` 的响应和错误语义。
- 使用真实 PostgreSQL 和 Redis 验证 Alembic 升级、原子创建和身份解析。
- 验证 Redis 认证限流的允许、超限 `429` 与依赖不可用 `503` 路径。
- 发布前复验迁移可重复升级至 `head`，且敏感值不出现在日志和响应中。
