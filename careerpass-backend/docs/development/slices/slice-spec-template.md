# Slice XX — <切片名称>

> 当前阶段：Slice Select / Slice Design / Readiness Check / Implement / Verify / Close
>
> 当前状态：in_progress / blocked / passed

## 1. 目标与范围

- 切片目标
- 本次实现范围
- 明确不实现的范围

## 2. 触发条件与完成结果

- 触发动作 / 触发事件
- 核心业务流程
- 最终可观察结果
- 稳定交接点

## 3. 前置条件与上下游衔接

- 前置条件
- 上游输入
- 下游输出
- 交接契约
- 上下游衔接责任

## 4. 现状与影响分析

- 现有代码 / 能力
- 可复用内容
- 缺失能力
- 需要修改的内容
- 对 API、领域模型、数据库、异步任务、前端等的影响

## 5. 领域实体确认与领域模型影响

本节必须列出本切片涉及的全部领域对象，包括只读查询、创建、修改和仅在运行时使用的对象，不得只记录接口输入输出。

| 对象 | 类型 | 本切片用途 | 查询/写入方式 | 归属关系 | 来源证据 | 处理 |
| --- | --- | --- | --- | --- | --- | --- |
| `<对象名称>` | 持久化实体 / 关联实体 / 值对象 / 运行时投影 | 查询、创建、修改或仅作上下文 | Repository、Service 或认证依赖 | 所属用户、候选人或其他实体 | 代码、迁移、测试或 Slice Design 裁决 | 已确认 / 新增 / 修改 / 延后 |

必须明确：

- 每个对象的职责，以及它是持久化实体、关联实体、值对象还是运行时投影；
- 本切片对对象执行查询、创建、修改还是仅作上下文使用；
- 对象之间的关系、所有者、授权边界和查询时必须校验的归属链；
- 对应的 Repository、数据访问路径和必要的事务边界；
- 新增或修改对象、字段、关系、状态和合法迁移时，需要同步的全局事实源；
- 若不更新 `docs/domain/domain-model.md`、`docs/data/database-design.md` 或 `docs/product/business-rules.md`，必须写明理由。

前端页面、流程和字段只能作为领域对象的发现线索，不能单独证明后端实体、持久化关系、状态迁移或授权规则。最终确认必须以当前代码、已执行迁移、测试或通过门禁的 Slice Design 为依据。

领域实体发生新增或变化时，必须在 Readiness Check 前完成对应全局文档同步；实现过程中发现模型变化，必须回退到 Slice Design。

## 6. 接口与数据契约

- API 请求 / 响应
- 异步任务输入 / 输出
- 领域状态、合法迁移和进度事件（状态属于 `docs/domain/domain-model.md`，本节记录本切片的具体应用）
- 错误语义
- 必要的数据约束

本节是本切片实际使用的 API 和异步任务契约唯一事实源；不再创建全局 API 或异步任务契约文档。

## 7. 技术方案

- 整体执行流程
- 涉及组件及职责
- 数据读写流程
- 异步处理方式
- 外部能力依赖
- 必要的关键技术决策

## 8. 就绪检查（Readiness Check）

- 前置切片与跨切片契约
- 关键技术问题裁决
- 外部依赖真实证据
- 全局文档同步
- 验收环境与测试数据
- Ready / blocked 结论

## 9. 实现范围

- [ ] 任务 1
- [ ] 任务 2
- [ ] 任务 3

## 10. 验证与关闭（Verify / Close）

- 单元测试
- API / 接口测试
- 集成测试
- 前后端端到端验收
- 完成判定标准
- 开发过程中发现但延期处理的问题
- 文档与最终实现一致性
- 切片完成 / 阻塞结论
